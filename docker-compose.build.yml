services:
  verify:
    build:
      context: .
      dockerfile: Dockerfile.build
    working_dir: /app
    volumes:
      - ./SOURCE_SNAPSHOT/RESTORE_SERVICE:/app
      - node_modules:/app/node_modules
    environment:
      CI: "1"
    tty: true
    stdin_open: true

volumes:
  node_modules:
