{
  "name": "appts-restore-service-kernel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.mjs tests/vectors/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
