import { useEffect, useMemo, useState } from "react";
import { createUiClient, type UiViewEnvelope } from "../api.ts";
import { ViewFrame } from "../components/ViewFrame.tsx";
function ticketId():string|undefined{const match=/^\/tickets\/([^/]+)\/closure/.exec(location.pathname);if(!match?.[1])return undefined;try{return decodeURIComponent(match[1]);}catch{return undefined;}}
function text(value:unknown):string|undefined{return typeof value==="string"&&value.length>0?value:undefined;}
export function ClosureReadinessView(){const id=useMemo(ticketId,[]);const[projection,setProjection]=useState<UiViewEnvelope>();const[failure,setFailure]=useState<string>();useEffect(()=>{if(!id){setFailure("TICKET_ID_MISSING");return;}const client=createUiClient();void client.read(`/tickets/${encodeURIComponent(id)}/concerns/closure`).then((value)=>{if(value.view_id!=="UX-RS-11")throw new Error("CLOSURE_VIEW_MISMATCH");setProjection(value);}).catch((error:unknown)=>setFailure(error instanceof Error?error.message:"CLOSURE_PROJECTION_UNAVAILABLE"));},[id]);const d=projection?.data;return <ViewFrame viewId="UX-RS-11" title="Terminal / Closure Readiness">{failure?<p role="alert">{failure}</p>:null}{!projection?<p>Loading server-derived terminal readiness.</p>:<section><p><strong>Lifecycle state:</strong> {text(d?.["current_state_code"])??"Unknown"}</p><p><strong>Terminal assessment:</strong> {text(d?.["terminal_assessment_result"])??"Not assessed"}</p><p><strong>Disposition qualifier:</strong> {text(d?.["disposition_qualifier_ref"])??"Not available"}</p><p><strong>Terminal authority:</strong> {text(d?.["terminal_disposition_authority_ref"])??"Not available"}</p><p><strong>Communication:</strong> {text(d?.["communication_disposition_ref"])??"Not available"}</p><p><strong>Day-2 boundary:</strong> {text(d?.["day2_boundary"])??"NO_CLOSE_ACTION_AUTHORIZED"}</p><p><strong>Closure posture:</strong> {text(d?.["closure_posture"])??"CLOSURE_NOT_AVAILABLE"}</p><p>Readiness is server-derived. Day-2 authority permits no arbitrary complete control and no CLOSE action.</p>{id?<p><a href={`/tickets/${encodeURIComponent(id)}`}>Return to Ticket Console</a></p>:null}</section>}</ViewFrame>;}
