# Managed by Ansible — Caddy reverse proxy for the appts staging stack.
# Routes staging.ts.cifo.id → api:8080 over the dep-ingress network and
# terminates TLS via Let's Encrypt (email from the ACME_EMAIL env var).
{
	email {env.ACME_EMAIL}
}

{{ appts_domain }} {
	reverse_proxy api:8080
}