---
# ─────────────────────────────────────────────────────────────────────────────
# Deploy appts-restore-service DEP-001 staging stack to an AlmaLinux 9 VPS:
#   1. install Docker Engine + compose plugin
#   2. sync the DEP-001 workspace, render .env, build + start the compose stack
#   3. wait for /healthz and print container status
# Run:
#   ansible-playbook playbooks/deploy.yml --ask-vault-pass
# ─────────────────────────────────────────────────────────────────────────────
- name: Deploy appts-restore-service DEP-001 staging stack
  hosts: appts
  gather_facts: true
  roles:
    - role: firewall
      tags: [firewall]
    - role: docker
      tags: [docker]
    - role: appts
      tags: [appts]
