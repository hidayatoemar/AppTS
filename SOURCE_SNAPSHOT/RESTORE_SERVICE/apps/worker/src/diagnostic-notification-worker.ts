export interface DiagnosticNotificationRepository<Notification, Result> { loadQueued(): Promise<readonly Notification[]>; deliver(notification: Notification): Promise<Result>; persistResult(notification: Notification, result: Result): Promise<void>; trapDeliveryFailure(notification: Notification, failure: unknown): Promise<void>; }
export async function runDiagnosticNotificationWorker<Notification, Result>(repository: DiagnosticNotificationRepository<Notification, Result>): Promise<number> { const queued = await repository.loadQueued(); for (const notification of queued) { try { await repository.persistResult(notification, await repository.deliver(notification)); } catch (failure) { await repository.trapDeliveryFailure(notification, failure); } } return queued.length; }
