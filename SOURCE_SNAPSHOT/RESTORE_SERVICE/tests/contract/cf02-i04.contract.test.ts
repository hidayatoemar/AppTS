import test from "node:test";
import assert from "node:assert/strict";
import { I04_ACCESS_D02, I04_ACCESS_D03, I04_COMPOUND_PROFILE_IDENTITY, I04_INTERFACE_IDENTITY, I04_SEMANTIC_VERSION, validateCompoundContext } from "../../packages/contracts/src/i04-compound-context.ts";
const valid = (access: typeof I04_ACCESS_D02 | typeof I04_ACCESS_D03) => ({ interface_identity: I04_INTERFACE_IDENTITY, semantic_version: I04_SEMANTIC_VERSION, profile_identity: I04_COMPOUND_PROFILE_IDENTITY, access_set_identity: access, message_id: "m", idempotency_key: "i", correlation_id: "c", producer_ref: "D-04", subject_ref: "ticket", source_sequence_or_version: 1, produced_at: "2026-01-02T03:04:05.006Z", currentness_ref: "current", payload_hash: "hash", payload: { compound_context_id: "cc", pairing_identity: "pair", ticket_id: "ticket", context_version: 1, aggregate_version: 1, purpose_binding_id: "purpose", domain_id: "domain", current_state_code: "ACTIVE", currentness_ref: "current", paired_component_versions: ["d02-1", "d03-1"], access_set_identity: access, access_set_payload: {} } });
test("CF05-CT-001 I04 exact identity version profile and access set", () => { assert.equal(validateCompoundContext(valid(I04_ACCESS_D02), I04_ACCESS_D02).ok, true); assert.equal(validateCompoundContext({ ...valid(I04_ACCESS_D02), semantic_version: "2.0.0" }, I04_ACCESS_D02).ok, false); });
test("CF05-CT-002 I04 required compound fields fail closed", () => { const value = valid(I04_ACCESS_D02); const { pairing_identity: _removed, ...payload } = value.payload; assert.equal(validateCompoundContext({ ...value, payload }, I04_ACCESS_D02).ok, false); });
test("CF05-CT-006 stale I04 without currentness fails closed", () => { const { currentness_ref: _removed, ...value } = valid(I04_ACCESS_D02); assert.equal(validateCompoundContext(value, I04_ACCESS_D02).ok, false); });
test("CF05-CT-011 mixed D02 D03 access pairing rejects the entire context", () => { const value = valid(I04_ACCESS_D02); assert.equal(validateCompoundContext({ ...value, payload: { ...value.payload, access_set_identity: I04_ACCESS_D03 } }, I04_ACCESS_D02).ok, false); assert.equal(validateCompoundContext(value, I04_ACCESS_D03).ok, false); });
